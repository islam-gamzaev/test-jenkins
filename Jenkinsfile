node {
        stage('Build') {
                sh 'pwd && ls -lh'
                echo 'Build some application'
        }
        stage('Test') {
                echo 'Test some application'
        }

        stage('Deploy') {
                echo 'Deploy some application'
        }
 }